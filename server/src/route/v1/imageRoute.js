// import express from "express";
// const router = express.Router();
// import { validateToken } from "../../middlewares/v1/auth";
// //import { authorize } from "../../middlewares/v1/authorize";
// import {
//   uploadImageHandler,
//   getAllImageHandler,
//   getImageHandler,
//   updateImageHandler,
//   deleteImageHandler,
// } from "../../controllers/v1/imageController";
// import upload from "../../middlewares/v1/mutlerUpload";



// router.post("/upload", validateToken, upload.single("image"), uploadImageHandler);
// router.get("", getAllImageHandler);
// router.get("/:id", getImageHandler);
// router.put("/:id", validateToken, updateImageHandler);
// router.delete("/:id", validateToken, deleteImageHandler);

// module.exports = router;

