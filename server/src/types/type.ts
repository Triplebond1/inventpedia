export type ImageType = {
    imageUrl: string,
    imageAltText: string,
    imageThumbnail: string,
    imageWidth: number,
    imageHeight: number,
  };
  
export type VideoType = {
    videoUrl: string,
    videoAltText: string,
    videoThumbnail: string,
    videoWidth: number,
    videoHeight: number,
  };